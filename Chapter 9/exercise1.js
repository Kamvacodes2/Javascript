'use strict'
console.dir(window);

//name ""
//document: document
//innerHeight: 599
//innerWidth:  196

// Window navigator object
// contains info about the browser we are using and version we are using
// this can be handy for customizing the website for certain operating systems

/**
 * !    console.dir(window.navigator);
 * ? we start with acccessing the window because navigator is a object of the window object
 * ! We can also access it through the following console.dir(navigator);
 */

// Window location object
// This contains the URL of the current web page
/**
 * !If you override (parts of) that property, you force the browser to go to a new page!
 * ? access through console.dir(window.location) or console.dir(location);
 */


